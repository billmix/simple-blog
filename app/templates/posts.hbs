<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-12">
            <h1 class="text-center">Get Yer Posts Here</h1>
        </div>
    </div>
    {{outlet}}
    <div class="row">
        {{#each model as |post|}}
          {{#if post.post_published }}
          <div class="col-md-4">
            {{post-card post.featured_image post post.title post.created_at post.content}}
          </div>
          {{/if}}
        {{/each}}

  </div>
</div>
